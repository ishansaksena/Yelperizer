# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Yelperizer' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Yelperizer
  pod 'Alamofire', '~> 3.4'
  pod 'SwiftyJSON'
  pod 'Firebase'
  pod 'FirebaseAuth'
  pod 'FirebaseDatabase'
  target 'YelperizerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'YelperizerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
